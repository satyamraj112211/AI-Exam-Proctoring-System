<%- include('partials/layout', { title, body: `
  <div class="card">
    <h1 style="margin-top: 0; margin-bottom: 8px;">Student Dashboard</h1>
    <p class="muted" style="margin-bottom: 16px;">
      Only users with the <strong>student</strong> role can access this route.
      Authorization is enforced by middleware on the /student path.
    </p>

    <p>
      In a full system, this is where students would see upcoming exams,
      active tests, and historical results.
    </p>
  </div>
` }) %>



















