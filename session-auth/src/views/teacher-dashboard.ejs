<%- include('partials/layout', { title, body: `
  <div class="card">
    <h1 style="margin-top: 0; margin-bottom: 8px;">Teacher Dashboard</h1>
    <p class="muted" style="margin-bottom: 16px;">
      Only users with the <strong>teacher</strong> role can access this route.
      Authorization is enforced by middleware on the /teacher path.
    </p>

    <p>
      In a full system, this is where teachers would see the exams they are assigned
      as proctors, monitor live sessions, and review student attempts.
    </p>
  </div>
` }) %>



















